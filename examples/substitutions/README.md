## Example: Basic usage

Run `./gradlew clean` and see the console outputs.

This project uses a template `.env` file to define default environment variables sith substitutions.

Edit `build.gradle.kts` or `.env`.

Then run to see if how it works.
